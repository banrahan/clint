name: "smoke-test"
command: "echo 'Hello from clint' && sleep 1 && echo 'Done'"
cwd: "/tmp"

goals:
  - "Wait for 'Hello from clint' to appear"
  - "Wait for 'Done' to appear"
  - "Take a screenshot of the final state"
