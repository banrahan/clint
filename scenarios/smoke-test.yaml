name: "smoke-test"
command: "echo 'Hello from auto-test-tool' && sleep 1 && echo 'Done'"
cwd: "/tmp"
step_timeout: 10
env: {}
steps:
  - expect: "Hello from auto-test-tool"
    action: wait
    screenshot: true
    delay_after: 2
  - expect: "Done"
    action: wait
    screenshot: true
